services:
  lumen:
    image: ghcr.io/1797127235/lumen:latest
    build: .
    container_name: lumen
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - lumen-data:/root/.lumen
    restart: unless-stopped

volumes:
  lumen-data:
    driver: local
