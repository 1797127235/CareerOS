# 交互设计与数据模型

> 源自：功能模块与交互设计_AI职业规划智能体.md

## 三、交互流程与信息架构

### 3.1 系统核心用户旅程

```
【认知阶段】注册 -> 新手引导 -> 方向测评/咨询 -> 生成四年规划
     |
     v
【积累阶段】学习路径生成 -> 日常学习追踪 -> 能力差距分析 -> 项目实践
     |
     v
【求职阶段】简历优化 -> 模拟面试训练 -> 求职进度管理 -> 面试准备提醒
     |
     v
【决策阶段】Offer评估 -> 决策报告 -> 职业起点确认 -> 进入【职业成长期】
```

#### 关键路径详细说明

**路径A：大一新生"从迷茫到清晰"**
1. 注册后新手引导（3步：设置年级->选择大致兴趣->选择目标层次）
2. 首页推荐"行业全景导览"，用户点击开始对话
3. AI用大白话介绍各方向，根据用户互动推荐【智能方向测评】
4. 完成测评，获得Top3方向推荐 + 匹配雷达图
5. AI询问"要不要为你生成大学四年规划？"
6. 选择目标方向 -> 生成四年规划 -> 保存到个人中心
7. 系统推送首周学习任务 -> 进入日常学习循环

**路径B：大三学生"从焦虑到行动"**
1. 注册后设置目标岗位（如"字节后端"）
2. 系统推荐【岗位能力差距分析】
3. 粘贴JD -> 完成测评 -> 获得差距报告
4. 点击"生成补全路径" -> 获得针对性学习路线
5. 日常学习中遇到问题 -> 【AI对话伙伴】实时答疑
6. 项目完成后 -> 【项目实践指导】包装经历
7. 求职季开启 -> 【简历优化】+【模拟面试】+【求职进度管理】联动

**路径C：大四求职者"冲刺上岸"**
1. 已有目标公司列表，直接使用【求职进度管理】创建看板
2. 为每家公司生成定制简历（【简历优化】联动）
3. 面试前使用【模拟面试】突击薄弱点
4. 面试后记录反馈，系统自动关联到看板
5. 系统根据面试反馈动态调整推荐复习内容
6. 拿到Offer后 -> 【Offer决策分析器】辅助选择

### 3.2 信息架构（导航结构）

```
+-------------------------------------------------------------+
|  [Logo]  首页    职业认知    学习成长    求职作战    AI对话   [头像] |
+-------------------------------------------------------------+
|                                                              |
|  【首页】个性化仪表盘（根据用户阶段动态变化）                   |
|    |-- 今日待办（学习任务/面试提醒/投递截止）                   |
|    |-- 进度概览（学习进度环/求职漏斗图/能力雷达图）             |
|    |-- 快捷入口（继续上次对话/开始模拟面试/查看规划）            |
|    |-- 推荐内容（基于画像的个性化推荐）                        |
|                                                              |
|  【职业认知】                                                  |
|    |-- 行业全景（方向导览卡片列表）                            |
|    |-- 方向测评（互动测评入口）                                |
|    |-- 我的规划（四年规划查看与调整）                          |
|    |-- 行业数据（岗位/薪资/技能趋势看板）                       |
|                                                              |
|  【学习成长】                                                  |
|    |-- 我的路径（当前学习路径可视化）                          |
|    |-- 能力诊断（差距分析与测评记录）                           |
|    |-- 项目实践（项目推荐与指导）                              |
|    |-- 资源库（学习资源聚合与搜索）                            |
|                                                              |
|  【求职作战】                                                  |
|    |-- 我的简历（简历版本管理与优化）                          |
|    |-- 模拟面试（八股/算法/系统/行为/综合）                      |
|    |-- 求职看板（进度管理与数据复盘）                           |
|    |-- Offer决策（多Offer评估工具）                             |
|    |-- 校招日历（时间节点聚合）                                |
|                                                              |
|  【AI对话】统一对话入口                                        |
|    |-- 对话列表（历史对话记录）                                |
|    |-- 快捷话题（预设高频话题入口）                             |
|    |-- 收藏内容（对话中收藏的要点）                            |
|                                                              |
|  【个人中心】                                                  |
|    |-- 我的画像（基础信息与目标设置）                           |
|    |-- 学习档案（历史路径/成绩/能力演变）                        |
|    |-- 求职档案（投递/面试/Offer记录）                          |
|    |-- 成就徽章（成长激励体系）                                 |
|    |-- 设置（通知/隐私/账号管理）                              |
|                                                              |
+-------------------------------------------------------------+
```

### 3.3 关键页面布局与交互说明

#### 页面1：首页仪表盘（个性化动态首页）

```
+----------------------------------------+
|  [顶部栏] Logo + 主导航 + 用户头像      |
+----------------------------------------+
|  欢迎语："早啊，今天的学习进度是68%"      |
|  +------------+  +------------------+  |
|  | 今日待办    |  |  本周进度概览     |  |
|  | * 面试提醒   |  |  [环形进度图]    |  |
|  | * 学习任务   |  |  学习 3/5 项     |  |
|  | * 投递截止   |  |  求职 2/8 家     |  |
|  | [查看全部]  |  |  复习 1/3 项     |  |
|  +------------+  +------------------+  |
|  +----------------------------------+   |
|  | 快捷入口（自适应用户阶段）         |   |
|  | [继续学习] [模拟面试] [AI咨询] [简历优化] |   |
|  +----------------------------------+   |
|  +----------------------------------+   |
|  | 为你推荐                         |   |
|  | * 文章：算法岗2025年还值得入吗？  |   |
|  | * 练习：Redis高频面试题Top20      |   |
|  | * 提醒：下周开始大厂提前批         |   |
|  +----------------------------------+   |
+----------------------------------------+
```

**交互说明**:
- 首页内容根据用户画像动态组装（低年级重规划，高年级重求职）
- 待办事项支持滑动完成，完成后有微动效和正向反馈
- 环形进度图点击可下钻到对应模块详情

#### 页面2：AI对话界面（全系统核心交互界面）

```
+----------------------------------------+
|  [顶部栏] 返回 | 对话主题 | 更多选项   |
+----------------------------------------+
|                                       |
|  [系统头像] 嗨，我是你的职业学长Agent   |
|            今天想聊聊什么？              |
|            [后端方向] [学习路线] [简历]   |
|            [校招时间] [求安慰]           |
|                                       |
|  ------------------------------------   |
|  [用户气泡] 我想了解后端开发方向        |
|  ------------------------------------   |
|  [系统气泡] 后端开发主要负责...（展开）  |
|            [追问：需要学什么技术？]      |
|            [追问：薪资怎么样？]         |
|            [追问：我不爱写代码适合吗？]   |
|                                       |
|  ------------------------------------   |
|  [系统卡片] "后端学习路线"               |
|            [点击展开可视化路径]          |
|            [保存到学习档案]             |
|  ------------------------------------   |
|                                       |
+----------------------------------------+
|  [快捷话题栏] 常见问题快捷入口（可滑动）  |
|  +----------------------------------+  |
|  | [语音] 今天聊聊...          [发送] |  |
|  +----------------------------------+  |
+----------------------------------------+
```

**交互说明**:
- 对话支持Markdown渲染（代码块、列表、表格）
- 系统输出中的卡片支持交互（如路径卡片可展开、报告卡片可下载）
- 追问按钮点击后立即发送，减少用户输入成本
- 长按系统消息可"收藏"或"反馈回答质量"
- 输入框支持"@"快捷调用功能（如"@简历优化 帮我看看这份简历"）

#### 页面3：求职看板（可视化进度管理）

```
+----------------------------------------+
|  求职看板 - 2025秋招                    |
|  [漏斗概览] 已投42 -> 笔12 -> 面5 -> OC2   |
+----------------------------------------+
|  +---------+ +---------+ +---------+ |
|  | 已投递   | | 面试中   | | 已Offer  | |
|  |  [15]   | |  [5]    | |  [2]    | |
|  |         | |         | |         | |
|  | # 字节   | | # 美团   | | # 腾讯   | |
|  | # 阿里   | | # 京东   | | # 快手   | |
|  | # 腾讯   | | # 百度   | |         | |
|  | + 添加   | | + 添加   | | + 添加   | |
|  +---------+ +---------+ +---------+ |
|                                       |
|  [数据复盘] [面试日历] [策略建议]      |
+----------------------------------------+
```

**交互说明**:
- 支持拖拽卡片在不同状态列间移动
- 点击卡片展开详情（岗位信息、简历版本、面试记录、反馈）
- 双击卡片快速编辑状态
- 看板支持列表/看板/日历三种视图切换
- 底部常驻"+ 记录投递"FAB按钮

### 3.4 AI对话交互设计原则

#### 原则1："学长口吻" — 亲切专业不冰冷

| 场景 | 错误示例（冰冷） | 正确示例（学长口吻） |
|------|---------------|-------------------|
| 方向介绍 | "后端开发是负责服务器端业务逻辑处理的软件工程师岗位" | "后端就是处理你点外卖时'订单怎么创建、优惠券怎么算、支付怎么对接'这些事儿的。用户看不到后端，但没有后端，App就是空壳子。" |
| 学习建议 | "建议先学习Java SE基础，预计耗时4周" | "先别急着搞微服务，Java基础不牢地动山摇。老老实实4周把Java SE啃完，我当年就是跳过集合源码直接上Spring，面试被问到HashMap源码直接懵了。" |
| 焦虑安抚 | "请保持积极心态，继续投递简历" | "我秋招投了80多家才拿5个Offer，你这才20家急啥。不过咱们看看数据，是不是简历过筛率有点低？我给你分析分析。" |

#### 原则2：主动追问 > 被动回答

- 不直接回答模糊问题，而是先澄清："你是想了解前端技术栈有哪些，还是想知道前端未来的发展前景？"
- 回答后主动延伸："说到这儿，你可能还想知道..."
- 关键信息遗漏时主动确认："为了给你更准确的建议，你现在是大几？学过哪些编程语言？"

#### 原则3：结构化输出降低认知负荷

- 长回答必须分段：【一句话总结】【详细解释】【给你的建议】【下一步】
- 复杂内容用信息卡片承载（如路径卡片、雷达图卡片、对比表格卡片）
- 关键数据高亮（薪资范围、时间节点、成功率）
- 支持"展开更多"折叠长篇内容

#### 原则4：即时反馈与操作入口

- 回答中涉及的功能提供"一键跳转"按钮（如"生成学习路径""开始模拟面试"）
- 对话中可执行的结论提供"保存""导出""分享"操作
- 用户操作后给予即时确认（"已保存到你的学习档案"）

#### 原则5：渐进式披露降低门槛

- 首次对话不超过3轮即给出可见价值（如方向推荐、初步建议）
- 复杂功能分步引导（如测评分模块完成，不用一次做完）
- 高级功能默认隐藏，用户需要时展开（如"查看完整技术图谱"）

---

## 四、数据模型设计

### 4.1 核心实体定义

系统核心实体共 **10个**：

| 实体 | 英文名称 | 定义 | 核心作用 |
|------|---------|------|---------|
| 用户 | `User` | 系统注册用户 | 身份与权限基础 |
| 用户画像 | `UserProfile` | 用户职业相关信息 | 个性化推荐的数据基础 |
| 对话会话 | `Conversation` | 一次AI对话的完整记录 | 多轮对话状态管理 |
| 对话消息 | `Message` | 对话中的单条消息 | 内容存储与上下文构建 |
| 学习路径 | `LearningPath` | 用户的学习路线规划 | 学习进度追踪基础 |
| 路径节点 | `PathNode` | 学习路径中的单个学习单元 | 任务拆解与进度标记 |
| 简历 | `Resume` | 用户的简历数据 | 解析与优化的数据基础 |
| 求职记录 | `JobApplication` | 单次求职投递记录 | 求职进度追踪 |
| 面试记录 | `InterviewRecord` | 单次面试的详细记录 | 反馈分析与复盘 |
| 能力评估 | `SkillAssessment` | 用户的技能评估结果 | 差距分析基础 |

### 4.2 实体关系图（ER图文字描述）

```
+----------+       +--------------+       +----------------+
|   User   |<----->|  UserProfile |<----->|  SkillAssessment|
|  (用户)   | 1:1   |  (用户画像)   | 1:N   |   (能力评估)    |
+----------+       +--------------+       +----------------+
       |                 |
       | 1:N             | 1:N
       v                 v
+--------------+    +--------------+
| Conversation |    | LearningPath |
|  (对话会话)   |    |   (学习路径)  |
+--------------+    +--------------+
       | 1:N              | 1:N
       v                  v
+--------------+    +--------------+
|   Message    |    |   PathNode   |
|  (对话消息)   |    |   (路径节点)  |
+--------------+    +--------------+
                            |
       +--------------------+--------------------+
       | 1:N                | 1:N                | 1:N
       v                    v                    v
+--------------+    +--------------+    +--------------+
|    Resume    |    |JobApplication|    |   Resource   |
|    (简历)     |    |   (求职记录)  |    |   (资源)     |
+--------------+    +--------------+    +--------------+
                           | 1:N
                           v
                    +--------------+
                    |InterviewRecord|
                    |  (面试记录)   |
                    +--------------+
```

**关系说明**:
- `User` 1:1 `UserProfile`：每个用户有且仅有一个画像（注册时创建，持续更新）
- `User` 1:N `Conversation`：用户可有多段对话历史
- `Conversation` 1:N `Message`：每段对话包含多条消息
- `UserProfile` 1:N `LearningPath`：用户可有多条学习路径（不同方向/版本）
- `LearningPath` 1:N `PathNode`：每条路径包含多个学习节点
- `UserProfile` 1:N `SkillAssessment`：用户可进行多次能力评估
- `User` 1:N `Resume`：用户可有多版本简历
- `User` 1:N `JobApplication`：用户可投递多个岗位
- `JobApplication` 1:N `InterviewRecord`：每次求职可有多轮面试
- `PathNode` N:M `Resource`：学习节点关联多个学习资源

### 4.3 关键实体字段定义

#### 实体1：User（用户）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `user_id` | UUID | 主键，用户唯一标识 |
| `phone` | VARCHAR(20) | 手机号，登录账号 |
| `email` | VARCHAR(100) | 邮箱（可选） |
| `nickname` | VARCHAR(50) | 用户昵称 |
| `avatar_url` | VARCHAR(500) | 头像URL |
| `status` | ENUM | 状态：active/suspended/deleted |
| `created_at` | TIMESTAMP | 注册时间 |
| `last_login_at` | TIMESTAMP | 最后登录时间 |
| `user_type` | ENUM | 用户类型：student/transfer_student/graduate |
| `privacy_level` | ENUM | 隐私级别：standard/high（未成年人默认high） |

#### 实体2：UserProfile（用户画像）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `profile_id` | UUID | 主键 |
| `user_id` | UUID | 外键 -> User |
| `school_name` | VARCHAR(100) | 学校名称 |
| `school_level` | ENUM | 院校层级：985/211/double_first_class/normal |
| `major` | VARCHAR(50) | 专业 |
| `grade` | ENUM | 年级：freshman/sophomore/junior/senior/graduate1/graduate2/graduate3 |
| `graduation_year` | INT | 预计毕业年份 |
| `target_direction` | VARCHAR(50) | 目标方向（如"Java后端"） |
| `target_company_level` | ENUM | 目标公司层级：top/major/medium/state-owned |
| `current_skills` | JSON | 当前技能列表 [{"skill":"Java","level":3}] |
| `available_time_daily` | INT | 每日可用学习时长（小时） |
| `personality_tags` | JSON | 性格标签（从测评中提取） |
| `learning_style` | ENUM | 学习风格：video/reading/practice/mixed |
| `anxiety_level` | INT | 焦虑指数（1-10，由系统动态评估） |
| `preferred_interaction` | ENUM | 偏好交互：chat/detailed/brief |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

#### 实体3：Conversation（对话会话）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `conversation_id` | UUID | 主键 |
| `user_id` | UUID | 外键 -> User |
| `title` | VARCHAR(200) | 会话标题（AI自动生成，如"Java后端方向咨询"） |
| `topic_type` | ENUM | 话题类型：career_consult/learning/ resume/interview/emotional/technical_qa |
| `status` | ENUM | 状态：active/closed/archived |
| `context_snapshot` | JSON | 会话关键上下文（用于快速恢复） |
| `message_count` | INT | 消息数量 |
| `created_at` | TIMESTAMP | 创建时间 |
| `last_message_at` | TIMESTAMP | 最后消息时间 |
| `is_pinned` | BOOLEAN | 是否置顶 |

#### 实体4：Message（对话消息）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `message_id` | UUID | 主键 |
| `conversation_id` | UUID | 外键 -> Conversation |
| `role` | ENUM | 角色：user/assistant/system/tool |
| `content` | TEXT | 消息内容（Markdown格式） |
| `content_type` | ENUM | 内容类型：text/code/image/card/file |
| `card_type` | VARCHAR(50) | 卡片类型（如"learning_path"/"radar_chart"/"report"） |
| `card_payload` | JSON | 卡片数据（结构化内容） |
| `intent` | VARCHAR(50) | 用户意图标签（如"ask_direction""request_path"） |
| `sentiment` | FLOAT | 情感分数（-1到1，用于识别焦虑/积极） |
| `tokens_used` | INT | 消耗的token数 |
| `model_version` | VARCHAR(50) | 使用的模型版本 |
| `created_at` | TIMESTAMP | 发送时间 |
| `feedback_rating` | INT | 用户反馈评分（1-5，可选） |
| `feedback_comment` | TEXT | 用户反馈文字（可选） |

#### 实体5：LearningPath（学习路径）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `path_id` | UUID | 主键 |
| `user_id` | UUID | 外键 -> User |
| `profile_id` | UUID | 外键 -> UserProfile |
| `path_name` | VARCHAR(100) | 路径名称（如"Java后端大厂冲刺路线"） |
| `target_role` | VARCHAR(100) | 目标岗位 |
| `target_company` | VARCHAR(100) | 目标公司（可选） |
| `current_level` | VARCHAR(50) | 当前基础描述 |
| `total_duration_days` | INT | 预计总天数 |
| `daily_time_hours` | INT | 每日学习时长 |
| `start_date` | DATE | 计划开始日期 |
| `target_date` | DATE | 目标完成日期 |
| `status` | ENUM | 状态：draft/active/paused/completed/abandoned |
| `progress_percent` | INT | 整体进度百分比 |
| `completed_nodes` | INT | 已完成节点数 |
| `total_nodes` | INT | 总节点数 |
| `version` | INT | 路径版本号（每次调优+1） |
| `is_auto_adjusted` | BOOLEAN | 是否经过自动调优 |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

#### 实体6：PathNode（路径节点）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `node_id` | UUID | 主键 |
| `path_id` | UUID | 外键 -> LearningPath |
| `node_name` | VARCHAR(200) | 节点名称（如"MySQL索引原理"） |
| `node_type` | ENUM | 类型：knowledge/practice/project/quiz/milestone |
| `stage_number` | INT | 所属阶段序号 |
| `stage_name` | VARCHAR(100) | 阶段名称 |
| `sequence_order` | INT | 阶段内顺序 |
| `description` | TEXT | 节点描述 |
| `acceptance_criteria` | TEXT | 验收标准 |
| `estimated_hours` | INT | 预计学习时长 |
| `resource_refs` | JSON | 关联资源ID列表 |
| `prerequisite_nodes` | JSON | 前置节点ID列表（依赖关系） |
| `status` | ENUM | 状态：locked/unlocked/in_progress/completed/skipped |
| `started_at` | TIMESTAMP | 开始时间 |
| `completed_at` | TIMESTAMP | 完成时间 |
| `user_notes` | TEXT | 用户笔记 |
| `quiz_score` | INT | 节点测验得分（如有） |

#### 实体7：Resume（简历）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `resume_id` | UUID | 主键 |
| `user_id` | UUID | 外键 -> User |
| `resume_name` | VARCHAR(100) | 简历名称（如"张三_后端_字节_v2"） |
| `raw_file_url` | VARCHAR(500) | 原始文件URL |
| `raw_file_type` | ENUM | 文件类型：pdf/doc/docx/image |
| `parsed_content` | JSON | 解析后的结构化内容（教育/经历/项目/技能） |
| `optimized_content` | JSON | 优化后的内容（多版本） |
| `target_direction` | VARCHAR(50) | 目标方向 |
| `ats_score` | INT | ATS友好度评分 |
| `match_jd_id` | UUID | 关联的JD（可选） |
| `match_score` | INT | JD匹配度评分 |
| `is_default` | BOOLEAN | 是否默认简历 |
| `version` | INT | 版本号 |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

#### 实体8：JobApplication（求职记录）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `application_id` | UUID | 主键 |
| `user_id` | UUID | 外键 -> User |
| `company_name` | VARCHAR(100) | 公司名称 |
| `company_type` | ENUM | 公司类型：top/major/medium/state-owned/startup |
| `position_name` | VARCHAR(100) | 岗位名称 |
| `department` | VARCHAR(100) | 部门（可选） |
| `jd_text` | TEXT | JD原文 |
| `jd_parsed` | JSON | 解析后的JD要求 |
| `channel` | ENUM | 投递渠道：official/ referral/boss/zhaopin/others |
| `referrer_name` | VARCHAR(50) | 内推人姓名（如有） |
| `resume_id` | UUID | 使用的简历版本 |
| `current_stage` | ENUM | 当前阶段：applied/screening/written_test/1st_interview/2nd_interview/3rd_interview/hr_interview/oc/rejected/withdrawn/accepted |
| `stage_history` | JSON | 阶段变更历史 [{"stage":"","time":"","note":""}] |
| `priority` | ENUM | 优先级：high/medium/low |
| `applied_at` | TIMESTAMP | 投递时间 |
| `next_event_at` | TIMESTAMP | 下个事件时间（如笔试/面试） |
| `next_event_type` | ENUM | 下个事件类型 |
| `notes` | TEXT | 备注 |
| `is_active` | BOOLEAN | 是否仍在流程中 |
| `created_at` | TIMESTAMP | 创建时间 |
| `updated_at` | TIMESTAMP | 更新时间 |

#### 实体9：InterviewRecord（面试记录）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `record_id` | UUID | 主键 |
| `application_id` | UUID | 外键 -> JobApplication |
| `round_number` | INT | 面试轮次 |
| `round_type` | ENUM | 类型：phone/online_1st/online_2nd/online_3rd/onsite/hr |
| `interview_date` | TIMESTAMP | 面试日期 |
| `duration_minutes` | INT | 面试时长 |
| `questions_asked` | JSON | 问到的问题列表 |
| `performance_rating` | INT | 自评表现（1-5） |
| `difficulty_rating` | INT | 难度评级（1-5） |
| `key_takeaways` | TEXT | 关键收获 |
| `weaknesses_found` | JSON | 暴露的薄弱点 |
| `result` | ENUM | 结果：pending/passed/rejected/no_show |
| `feedback_from_company` | TEXT | 公司反馈（如有） |
| `created_at` | TIMESTAMP | 创建时间 |

#### 实体10：SkillAssessment（能力评估）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| `assessment_id` | UUID | 主键 |
| `user_id` | UUID | 外键 -> User |
| `profile_id` | UUID | 外键 -> UserProfile |
| `target_role` | VARCHAR(100) | 评估目标岗位 |
| `target_jd_text` | TEXT | 目标JD原文 |
| `assessment_type` | ENUM | 评估方式：self_rating/quiz/comprehensive |
| `dimension_scores` | JSON | 各维度得分 [{"dimension":"Redis","score":35,"max":100}] |
| `overall_score` | INT | 综合得分 |
| `match_percentage` | INT | 岗位匹配度百分比 |
| `gap_analysis` | JSON | 差距分析 [{"skill":"","gap_level":"","suggestion":"","duration_days":0}] |
| `radar_chart_data` | JSON | 雷达图数据 |
| `generated_path_id` | UUID | 生成的补全路径ID |
| `created_at` | TIMESTAMP | 创建时间 |

### 4.4 数据流转与状态机

#### 状态机1：学习路径状态流转

```
                    +-------------+
         +-------->|   draft     |<--------+
         |          |  （草稿）    |         |
         |          +------+------+         |
         |                 | 用户激活        |
         |                 v                |
         |          +-------------+         |
         |     +----|   active    |----+    |
         |     |    |  （进行中）  |    |    |
         |     |    +------+------+    |    |
         |     |           |           |    |
    放弃/过期 |     |    完成节点    暂停学习    |    | 重新激活
         |     |           |           |    |
         |     v           v           v    |
         |  +------+   +--------+   +-----+  |
         +--|abandoned|   |completed|   |paused|--+
            | （已放弃）|   |（已完成）|   |（暂停）|
            +--------+   +---------+   +------+
```

**状态说明与流转条件**:
| 状态 | 说明 | 流转条件 |
|------|------|---------|
| draft | 用户创建但未确认激活 | 用户点击"开始执行" -> active |
| active | 用户正在执行中 | 完成所有节点 -> completed；用户点击暂停 -> paused；用户点击放弃 -> abandoned |
| paused | 用户暂停执行 | 用户点击继续 -> active |
| completed | 所有节点完成 | 不可流转（终态） |
| abandoned | 用户主动放弃或过期 | 不可流转（终态） |

#### 状态机2：求职进度状态流转

```
+----------+    +----------+    +------------+    +----------+
| applied  |--->| screening|--->|written_test|--->|1st_intervw|
| （已投递） |    |（简历筛选）|    |  （笔试）   |    |（一面）    |
+----------+    +----------+    +------------+    +-----+-----+
     |                                                   |
     |    +-----------------------------------------------+
     |    |
     |    v    +----------+    +----------+    +----------+
     |    +--->|2nd_intervw|--->|3rd_intervw|--->|hr_intervw|
     |         |（二面）    |    |（三面）    |    |（HR面）   |
     |         +----------+    +----------+    +-----+-----+
     |                                               |
     |                                               v
     |                                          +----------+
     |                                          |    oc     |
     |                                          | （Offer）  |
     |                                          +-----+-----+
     |                                                |
     |         +--------------------------------------+
     |         |
     v         v
+------------------------+
|      rejected            |
|      （已拒绝/已挂掉）    |
|  任一环节均可流转至此      |
+------------------------+
```

**关键流转规则**:
- 任意正向状态均可流转至 `rejected`（公司拒绝）或 `withdrawn`（用户主动撤回）
- `oc` 后可流转至 `accepted`（用户接受）或继续比较（保持在oc状态）
- `screening` 可能直接跳过 `written_test`（部分公司无笔试）
- `stage_history` 字段记录完整流转轨迹

#### 状态机3：路径节点状态流转

```
+---------+    +---------+    +---------+    +---------+
| locked  |--->|unlocked |--->|in_progress|--->|completed|
|（未解锁） |    |（已解锁） |    |（学习中）  |    |（已完成） |
+---------+    +---------+    +---------+    +---------+
                    |                              |
                    +------------------------------+
                           （前置节点完成后自动解锁）
```

**解锁规则**:
- 节点的 `prerequisite_nodes` 中所有节点状态为 `completed` 时，自动解锁
- 用户可手动标记 `skipped`（跳过）但系统会警告"跳过此节点可能影响后续学习"

---

## 五、AI Agent工作流设计

### 5.1 Agent核心工作流程

系统采用 **"感知->理解->规划->行动->反馈"** 五阶段Agent工作流，所有AI交互均遵循此范式：

```
+---------------------------------------------------------------+
|  1. 感知（Perception）                                         |
|     |-- 用户输入捕获（文本/语音/图片）                            |
|     |-- 上下文加载（用户画像 + 对话历史 + 系统状态）               |
|     |-- 环境感知（当前时间、用户活跃状态、外部事件）               |
+---------------------------------------------------------------+
|  2. 理解（Understanding）                                      |
|     |-- 意图识别（分类用户请求类型）                              |
|     |-- 情感分析（焦虑/积极/困惑/愤怒）                          |
|     |-- 实体抽取（方向名、技术栈、公司名、时间节点）               |
|     |-- 缺失信息检测（判断是否需要追问补充）                       |
+---------------------------------------------------------------+
|  3. 规划（Planning）                                            |
|     |-- 任务拆解（将复杂请求拆分为子任务）                        |
|     |-- 工具选择（判断是否需要调用外部工具）                       |
|     |-- 策略选择（直接回答 / 引导追问 / 推荐功能 / 情感安抚）       |
|     |-- 知识检索（RAG检索相关领域知识）                          |
+---------------------------------------------------------------+
|  4. 行动（Action）                                              |
|     |-- LLM生成（基于Prompt工程生成结构化回答）                   |
|     |-- 工具调用（如需要，调用简历解析/路径生成/题库查询等工具）     |
|     |-- 卡片渲染（将结构化内容转化为前端可渲染卡片）                |
|     |-- 记忆更新（更新对话上下文和用户短期记忆）                  |
+---------------------------------------------------------------+
|  5. 反馈（Feedback）                                            |
|     |-- 输出格式化（Markdown + 卡片 + 追问按钮）                  |
|     |-- 用户操作入口（保存/导出/跳转/一键执行）                   |
|     |-- 追问推荐（基于当前话题生成2-3个追问选项）                   |
|     |-- 状态同步（将关键信息写入用户画像/学习档案等持久存储）        |
+---------------------------------------------------------------+
```

### 5.2 多轮对话的状态管理

#### 对话状态树

系统将每段对话维护为一棵 **状态树**，支持话题分支、回溯、切换：

```
Conversation: "Java后端方向咨询"
|
|-- [Root] 用户："后端开发是做什么的？"
|   |-- [Branch A] 系统：解释后端职责
|   |   |-- [Node A1] 用户："需要学什么技术？" -> 进入技术栈话题
|   |   |   |-- [Node A1-1] 系统：列出技术栈 -> 用户："Spring Boot难吗？"
|   |   |   |-- [Node A1-2] 系统：解释Spring Boot -> ...
|   |   |-- [Node A2] 用户："薪资怎么样？" -> 进入薪资话题
|   |-- [Branch B] 用户："和前端比哪个好？"
|       |-- [Node B1] 系统：后端vs前端对比 -> ...
|
|-- 当前活跃话题栈：[Root -> Branch A -> Node A1 -> Node A1-1]
  （用户说"回到刚才那个问题" -> 可回溯到任意历史节点）
```

#### 状态管理策略

| 状态类型 | 存储位置 | 说明 |
|---------|---------|------|
| 对话级状态 | Redis（TTL=24h） | 当前对话的活跃话题栈、已确认的用户信息、待办追问 |
| 会话级状态 | Conversation.context_snapshot | 会话的摘要快照，用于对话列表预览和快速恢复 |
| 用户级状态 | UserProfile + 专用缓存 | 跨对话的持久记忆（目标方向、已知技能、偏好设置） |
| 全局级状态 | 系统配置 + 知识库 | 所有用户共享的行业知识、岗位数据、学习资源 |

**上下文窗口管理**:
- 短对话（<10轮）：全量上下文送入LLM
- 长对话（>=10轮）：采用滑动窗口 + 关键信息摘要，保留最近6轮 + 对话摘要
- 话题切换时：保存原话题摘要，加载新话题上下文

### 5.3 工具调用场景与流程

系统Agent具备调用 **7类外部工具** 的能力：

| 工具类别 | 工具名 | 调用场景 | 输入 | 输出 |
|---------|--------|---------|------|------|
| 知识检索 | `knowledge_search` | 用户询问方向/技术/岗位信息时 | 查询关键词 | 结构化知识片段 |
| 路径生成 | `path_generator` | 用户请求学习路径时 | 目标岗位、当前基础、时间参数 | 完整学习路径JSON |
| 简历解析 | `resume_parser` | 用户上传简历时 | 文件URL/二进制 | 结构化简历JSON |
| 简历优化 | `resume_optimizer` | 用户请求简历优化时 | 简历JSON + JD文本 | 优化建议 + 优化后简历 |
| 测评引擎 | `quiz_engine` | 用户进行能力测评时 | 测评维度、难度 | 题目列表 + 评分逻辑 |
| 题库查询 | `question_bank` | 模拟面试出题时 | 方向、模块、难度 | 题目 + 答案 + 追问链 |
| 数据看板 | `market_data` | 用户询问行业数据时 | 方向、城市、时间范围 | 岗位/薪资/趋势数据 |

**工具调用流程示例**（用户请求"帮我生成Java后端学习路线"）：

```
用户输入："帮我生成Java后端学习路线"
    |
[感知] 捕获文本输入
    |
[理解] 意图=path_generation, 实体={direction:"Java后端"}
    |
[规划] 发现缺少必要参数（当前基础、可用时间）
       策略选择：先引导补充信息，再调用path_generator
    |
[行动] LLM生成追问："好嘞！为了给你定制最合适的路线，先问几个小问题：
       你现在Java基础怎么样？每天能投入多少时间学习？"
       -> 用户回答后，调用path_generator工具
    |
path_generator(目标岗位="Java后端", 当前基础="零基础", 每日时间=2h)
    |
返回路径JSON -> Agent转化为可视化卡片
    |
[反馈] 输出学习路径卡片 + "保存到学习档案"按钮 + 追问选项
    |
记忆更新：保存路径到LearningPath表，更新UserProfile.target_direction
```

### 5.4 个性化记忆机制

#### 记忆分层架构

```
+---------------------------------------------+
|  L1: 工作记忆（Working Memory）               |
|     存储：当前对话中的临时信息                   |
|     时效：单轮对话内                           |
|     示例：用户刚才提到的目标公司是字节           |
+---------------------------------------------+
|  L2: 短期记忆（Short-term Memory）             |
|     存储：最近3-5段对话的关键摘要               |
|     时效：7天内活跃对话                        |
|     示例：用户上周确定了Java后端方向             |
+---------------------------------------------+
|  L3: 长期记忆（Long-term Memory）              |
|     存储：用户画像、历史路径、评估结果            |
|     时效：永久（持续更新）                      |
|     示例：用户是大三双非院校，目标中厂           |
+---------------------------------------------+
|  L4: 事实记忆（Factual Memory）               |
|     存储：行业知识、岗位数据、学习资源            |
|     时效：定期更新（非用户特定）                 |
|     示例：2025年Java后端技术栈优先级排序        |
+---------------------------------------------+
```

#### 记忆检索与融合策略

**检索触发条件**:
1. **用户画像自动注入**：每次对话开始时，将用户核心画像（年级、方向、基础）注入System Prompt
2. **相关历史检索**：用户提到某方向/技术时，检索该话题的历史对话摘要
3. **进度状态查询**：用户询问学习/求职进展时，查询最新状态数据

**记忆融合Prompt示例**:
```
【用户背景】
- 张浩，大三，双非院校软件工程
- 目标方向：Java后端（上周确定）
- 当前基础：学过Java SE，Spring Boot入门
- 已生成学习路径：Java后端冲刺路线，当前进度23%
- 上次对话：询问Redis持久化机制，已解答

【当前话题】用户询问"微服务架构是什么"
【回复策略】结合用户基础水平（入门级），用通俗类比解释，
          关联已学知识（Spring Boot），提示学习路径中的后续节点
```

#### 记忆更新机制

| 触发事件 | 更新内容 | 存储位置 |
|---------|---------|---------|
| 用户明确目标方向 | `target_direction`, `target_company_level` | UserProfile |
| 用户提及新技能 | 追加到 `current_skills` | UserProfile |
| 完成能力测评 | 创建SkillAssessment记录，更新画像能力标签 | SkillAssessment + UserProfile |
| 生成学习路径 | 创建LearningPath记录 | LearningPath |
| 标记节点完成 | 更新PathNode.status, 重新计算path进度 | PathNode + LearningPath |
| 记录求职投递 | 创建JobApplication记录 | JobApplication |
| 面试后反馈 | 创建InterviewRecord，更新应用状态 | InterviewRecord + JobApplication |
| 对话中确认信息 | 更新对话上下文快照 | Conversation.context_snapshot |

---

## 六、用户体验设计原则

### 6.1 视觉风格建议

#### 整体风格定位

**"科技蓝 + 活力橙 + 温暖灰"** — 专业但不冰冷，年轻但不轻浮。

```
主色调：
  Primary: #2563EB（科技蓝）—— 代表技术、专业、信任
  Secondary: #F59E0B（活力橙）—— 代表行动、激励、温暖
  Accent: #10B981（成长绿）—— 代表进步、完成、正向反馈
  
背景色：
  浅色模式：#F8FAFC（极浅灰蓝）
  深色模式：#0F172A（深蓝黑）

文字色：
  标题：#1E293B（深 Slate）
  正文：#475569（中 Slate）
  辅助：#94A3B8（浅 Slate）
```

#### 风格原则

| 元素 | 设计建议 |
|------|---------|
| **界面整体** | 留白充足，卡片式布局，圆角（8px-12px），阴影柔和（微悬浮感） |
| **AI对话界面** | 用户气泡白色/浅色，AI气泡淡蓝色渐变，头像用 friendly 的卡通学长形象 |
| **数据可视化** | 雷达图、环形进度图、漏斗图采用品牌色渐变，数据变化带平滑动画 |
| **学习路径** | 时间线采用纵向/横向节点图，已完成节点绿色+对勾，进行中节点脉冲动画 |
| **求职看板** | Kanban风格，不同状态列用轻微色区分（投递=灰，面试=蓝，Offer=绿） |
| **图标系统** | 线性图标为主，关键操作用面性图标强调，避免过度装饰 |

#### 字体建议

- 中文：系统默认无衬线字体（PingFang SC / Microsoft YaHei）
- 英文/代码：JetBrains Mono（等宽，技术感）
- 标题层级：H1 24px / H2 20px / H3 16px / 正文 14px / 辅助 12px

### 6.2 情感化设计点

#### 设计目标：缓解焦虑、建立信任、激发动力

| 场景 | 情感化设计策略 | 具体实现 |
|------|--------------|---------|
| **用户表达迷茫** | 共情回应 + 给予掌控感 | AI不直接给答案，而是先肯定"迷茫很正常，我大一也完全不知道方向"，再引导"咱们一起梳理一下" |
| **用户表达焦虑** | 数据安抚 + 正常化 | "我统计过，平均投40份简历拿1个Offer，你现在投了20份没回复其实很正常。不过咱们看看是不是简历需要优化" |
| **用户完成里程碑** | 庆祝反馈 + 成长可视化 | 完成学习节点时播放微动效（彩带/徽章弹出），显示"你已超越67%的同阶段用户" |
| **用户面试挂掉** | 接纳情绪 + 建设性复盘 | "挂了很难受吧，我当年三面挂过阿里。但挂掉不是终点，咱们把这次的经验变成下次的武器" |
| **用户长期未登录** | 温和召回 + 进度提醒 | "好久不见！你的Java路线还停留在第3章，要一起接着学吗？只需要15分钟" |
| **深夜时段使用** | 关怀提醒 + 适度幽默 | "这么晚还在学习，注意身体啊。不过既然来了，给你出一道简单的题保持手感，然后早点休息" |

#### 信任感建立机制

1. **透明度**：AI明确标注"这是基于2024年数据""这部分建议仅供参考，建议你到XX再验证"
2. **可验证性**：所有数据建议提供来源链接（如薪资数据来自OfferShow，面经来自牛客）
3. **渐进式承诺**：首次使用不给用户压力，先提供小价值（回答一个问题），再逐步深入
4. **用户掌控感**：所有AI生成的内容用户都可编辑、调整、拒绝（"这个建议不太适合我"->AI重新生成）

#### 成就感体系（游戏化设计）

```
成就徽章示例：
- # 初识方向 — 完成方向测评
- # 路在脚下 — 生成第一条学习路径
- # 连续7天 — 连续7天学习打卡
- # 第一份简历 — 完成简历优化
- # 面试首秀 — 完成首次模拟面试
- # 百投百战 — 累计投递100家公司
- # Offer收割机 — 拿到第一个Offer

里程碑时间线：
  2025.03 -- 生成学习路径
  2025.05 -- 完成首个项目
  2025.07 -- 投递第一份简历
  2025.09 -- 拿到第一个Offer
```

### 6.3 无障碍与包容性设计

| 设计维度 | 具体措施 |
|---------|---------|
| **视觉无障碍** | 所有文字对比度 >= WCAG 2.1 AA标准（4.5:1）；支持系统级字体缩放；关键信息不只依赖颜色传达（如状态用图标+文字） |
| **屏幕阅读器** | 核心功能支持ARIA标签；对话内容结构化（角色标注、卡片语义化）；图片提供alt文本 |
| **认知包容** | 复杂功能提供"简单模式"（减少选项）；专业术语首次出现时提供 tooltip 解释；支持对话历史回溯 |
| **输入包容** | 支持语音输入（方便不方便打字场景）；支持图片上传（简历/笔记拍照）；对话容错（错别字、口语化表达均可理解） |
| **情绪包容** | 检测到用户表达负面情绪时，AI优先安抚而非直接给建议；提供"仅倾听"模式（用户可倾诉而不被打断） |
| **经济包容** | MVP核心功能免费；付费功能提供清晰的"价值说明"；学生认证后可获得折扣 |

---

## 附录A：核心功能快速索引

| 功能编号 | 功能名称 | 所在章节 |
|---------|---------|---------|
| M-01 | 智能职业咨询对话 | 二.① |
| M-02 | 个性化学习路径生成 | 二.② |
| M-03 | 岗位能力差距分析 | 二.③ |
| M-04 | 简历智能解析与优化 | 二.④ |
| M-05 | 技术八股/算法模拟面试 | 二.⑤ |
| M-06 | 求职进度管理 | 二.⑥ |

## 附录B：术语表

| 术语 | 说明 |
|------|------|
| RAG | Retrieval-Augmented Generation，检索增强生成，AI回答前先检索知识库 |
| ATS | Applicant Tracking System，简历自动筛选系统 |
| STAR | Situation-Task-Action-Result，行为面试回答框架 |
| JD | Job Description，岗位描述 |
| OC | Offer Call，发放录用通知 |
| HC | Head Count，招聘名额 |
| FAB | Floating Action Button，悬浮操作按钮 |

---

> **文档完成。** 本设计文档基于Stage 1需求分析和竞品调研，为系统开发提供功能架构、交互流程和数据模型基线。后续开发阶段可根据技术实现约束和用户反馈进行迭代调整。
